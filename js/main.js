let $ = (element) => document.querySelector(element)
let $$ = (elements) => document.querySelectorAll(elements)

$('.header__title__container').addEventListener('click', () => {
	console.log('hola')
})
